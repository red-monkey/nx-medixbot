import { TextVariant } from './types';

const fontFamily = 'ubuntu';

export const textVariants: TextVariant = {
  'page-heading': {
    fontFamily,
    fontSize: 20,
  },
  heading: {
    fontFamily,
    fontSize: 40,
  },
  subheading: {
    fontFamily,
    fontSize: 12,
  },
  body: {
    fontFamily,
    fontSize: 14,
  },
  button: {
    fontFamily,
    fontSize: 14,
  },
  caption: {
    fontFamily,
    fontSize: 12,
  },
};
