import { colors } from './colors';
import { textVariants } from './font';
import { space, borderWith, borderRadius } from './sizes';
import { Theme } from './types';

export const theme: Theme = {
  colors,
  textVariants,
  space,
  borderWith,
  borderRadius,
};

export * from './types';
